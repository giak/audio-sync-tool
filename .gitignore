# Données utilisateur — ne JAMAIS versionner
# (config, journal, cache, playlists, etc.)
data/

venv/
node_modules/
__pycache__/

# Build artifacts — .ts sources compile to .js (keep test files)
static/*.js
!static/*.test.js
