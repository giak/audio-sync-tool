# Track Ratings — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) to implement this plan task-by-task.

**Goal:** Add track ratings (0-100) to the playlist sidebar. Users press **N** on a focused track to open an inline input, type a value, press Enter to save. Ratings are persisted globally in `data/ratings.json` (keyed by fullPath), loaded on init, and displayed next to each track in the playlist panel.

**Architecture:** Backend adds `GET /ratings` + `PUT /ratings` to read/write `data/ratings.json`. Frontend adds `ratings.ts` module (CRUD, optimistic update), extends `renderPlaylistTracks()` to show rating + inline edit, and adds keyboard handler for **N** in `script.ts`.

**Tech Stack:** Python 3 + Flask (backend), HTML/CSS/JS vanilla (frontend), JSON files (storage).

## Global Constraints

- Flask + mutagen : uniques dépendances externes
- Aucune nouvelle dépendance — le rating JSON < 10 KB
- Optimistic update : la note s'affiche immédiatement, rollback si échec réseau
- Port : 8765

---

## Pre-flight Checklist (lecture obligatoire avant d'implémenter)

- [ ] Lire `docs/superpowers/specs/2026-06-23-ratings-design.md` — spec complète
- [ ] Lire `static/state.ts` — ajouter `ratings: Record<string, number>`
- [ ] Lire `static/render.ts` — `renderPlaylistTracks()` à modifier
- [ ] Lire `static/script.ts` — routage clavier, ajouter touche N
- [ ] Lire `static/api.ts` — module API HTTP existant
- [ ] Lire `static/style.css` — styles existants playlist
- [ ] Lire `app.py` — routes existantes, ajouter routes ratings
- [ ] Lire `test_app.py` — tests existants, ajouter tests ratings

---

### Task 1: Backend — Routes Ratings (app.py + tests)

**Files:**
- Modify: `app.py` — ajouter `RATINGS_PATH`, `GET /ratings`, `PUT /ratings`
- Modify: `test_app.py` — ajouter tests ratings

**Interfaces:**
- Produces: `GET /ratings` → `{"/path/to/file.mp3": 85}`, `PUT /ratings` → `{ok: true}`

- [ ] **Step 1: Ajouter RATINGS_PATH dans app.py**

```python
# Après PLAYLISTS_PATH = ..., ajouter :
RATINGS_PATH = os.path.join(DATA_DIR, 'ratings.json')
```

- [ ] **Step 2: Écrire les tests ratings avec pytest**

```python
# --- Ratings tests ---

def test_ratings_default_empty(client):
    """GET /ratings returns empty object when no ratings exist."""
    rv = client.get('/ratings')
    assert rv.status_code == 200
    assert rv.json == {}


def test_ratings_save_and_read(client):
    """PUT /ratings saves a rating, GET /ratings reads it back."""
    rv = client.put('/ratings', json={
        '/home/giak/Music/song.mp3': 85,
    })
    assert rv.status_code == 200
    assert rv.json == {'ok': True}

    rv = client.get('/ratings')
    assert rv.status_code == 200
    assert rv.json['/home/giak/Music/song.mp3'] == 85


def test_ratings_overwrite(client):
    """PUT /ratings overwrites existing rating for same path."""
    client.put('/ratings', json={'/path/song.mp3': 50})
    client.put('/ratings', json={'/path/song.mp3': 90})
    rv = client.get('/ratings')
    assert rv.json['/path/song.mp3'] == 90


def test_ratings_multiple_files(client):
    """PUT /ratings accepts multiple entries at once."""
    client.put('/ratings', json={
        '/path/a.mp3': 85,
        '/path/b.mp3': 72,
        '/path/c.mp3': 95,
    })
    rv = client.get('/ratings')
    assert len(rv.json) == 3
    assert rv.json['/path/a.mp3'] == 85
    assert rv.json['/path/b.mp3'] == 72
    assert rv.json['/path/c.mp3'] == 95


def test_ratings_invalid_value(client):
    """PUT /ratings returns 400 for out-of-range value."""
    rv = client.put('/ratings', json={'/path/song.mp3': 150})
    assert rv.status_code == 400

    rv = client.put('/ratings', json={'/path/song.mp3': -5})
    assert rv.status_code == 400

    rv = client.put('/ratings', json={'/path/song.mp3': 'abc'})
    assert rv.status_code == 400


def test_ratings_null_value_removes_key(client):
    """PUT /ratings with null value removes that key from ratings."""
    client.put('/ratings', json={'/path/song.mp3': 85})
    client.put('/ratings', json={'/path/song.mp3': None})
    rv = client.get('/ratings')
    assert '/path/song.mp3' not in rv.json


def test_ratings_persistence_across_requests(client):
    """Ratings persist between requests (no in-memory only)."""
    client.put('/ratings', json={'/path/song.mp3': 42})
    rv1 = client.get('/ratings')
    rv2 = client.get('/ratings')
    assert rv1.json == rv2.json
```

- [ ] **Step 3: Implémenter les routes ratings dans app.py**

```python
# Après AUDIO_EXT_MAP, avant les routes Playlist, ajouter :

RATINGS_PATH = os.path.join(DATA_DIR, 'ratings.json')


@app.route('/ratings', methods=['GET', 'PUT'])
def ratings():
    if request.method == 'PUT':
        data = request.json
        if not data or not isinstance(data, dict):
            return jsonify({'ok': False, 'error': 'Body must be a JSON object'}), 400

        ratings_data = load_json(RATINGS_PATH, {})

        for path, value in data.items():
            if value is None:
                ratings_data.pop(path, None)
            elif not isinstance(value, int) or value < 0 or value > 100:
                return jsonify({'ok': False, 'error': f'Valeur invalide pour {path}: {value}'}), 400
            else:
                ratings_data[path] = value

        save_json(RATINGS_PATH, ratings_data)
        return jsonify({'ok': True})

    return jsonify(load_json(RATINGS_PATH, {}))
```

- [ ] **Step 4: Exécuter les tests backend ratings**

```bash
cd /home/giak/projects/audio-sync-tool
./venv/bin/python -m pytest test_app.py -v -k "rating"
```

Expected: All rating tests pass.

- [ ] **Step 5: Exécuter tous les tests backend**

```bash
./venv/bin/python -m pytest test_app.py -v
```

Expected: All existing + new tests pass.

- [ ] **Step 6: Commit**

```bash
git add app.py test_app.py
git commit -m "feat(backend): ratings CRUD routes (GET/PUT /ratings)"
```

---

### Task 2: Frontend — État + Module ratings.ts

**Files:**
- Modify: `static/state.ts` — ajouter `ratings: Record<string, number>`
- Create: `static/ratings.ts` — module CRUD avec optimistic update
- Add: `static/ratings.test.ts` — tests unitaires

**Interfaces:**
- Produces: `ratings.ts` exported functions (loadRatings, saveRating, getRating, deleteRating)
- Consumes: `state.js` (state), `api.js` (api)

- [ ] **Step 1: Ajouter ratings dans state.ts**

Dans l'interface `AppState`, ajouter :

```typescript
// Après playlistFocus: PlaylistFocusZone
ratings: Record<string, number>;  // fullPath → note 0-100, chargé depuis GET /ratings
```

Dans l'objet `_state` initial, ajouter :

```typescript
// Après playlistFocus: 'source',
ratings: {},
```

- [ ] **Step 2: Créer static/ratings.ts**

```typescript
// ─── Ratings CRUD: per-file rating (0-100) with optimistic update ─────────

import { api } from './api.js';
import { state } from './state.js';

/**
 * Load all ratings from server into state.ratings.
 * Called on app init (in initApp) and after any save.
 */
export async function loadRatings(): Promise<void> {
  state.ratings = await api('/ratings');
}

/**
 * Get the rating for a specific file. Returns number or undefined.
 */
export function getRating(fullPath: string): number | undefined {
  return state.ratings[fullPath];
}

/**
 * Save a single rating. Updates state.ratings optimistically.
 * Rolls back on network/server error.
 * @returns true if saved successfully, false on error.
 */
export async function saveRating(fullPath: string, value: number): Promise<boolean> {
  if (!Number.isInteger(value) || value < 0 || value > 100) return false;

  // Snapshot for rollback
  const previous = state.ratings[fullPath];

  // Optimistic update
  state.ratings[fullPath] = value;

  const res = await api('/ratings', {
    method: 'PUT',
    body: JSON.stringify({ [fullPath]: value }),
  });

  if (!res.ok) {
    // Rollback
    if (previous !== undefined) {
      state.ratings[fullPath] = previous;
    } else {
      delete state.ratings[fullPath];
    }
    return false;
  }
  return true;
}

/**
 * Remove a rating for the given fullPath.
 * Sends a PUT with null value to the server.
 */
export async function deleteRating(fullPath: string): Promise<boolean> {
  const previous = state.ratings[fullPath];
  delete state.ratings[fullPath];

  const res = await api('/ratings', {
    method: 'PUT',
    body: JSON.stringify({ [fullPath]: null }),
  });

  if (!res.ok) {
    // Rollback
    if (previous !== undefined) {
      state.ratings[fullPath] = previous;
    }
    return false;
  }
  return true;
}
```

- [ ] **Step 3: Appeler loadRatings dans initApp**

Dans `actions.ts`, trouver la fonction `initApp()` et ajouter `await loadRatings()` après le chargement des playlists :

```typescript
// Dans actions.ts, dans la fonction initApp()
import { loadRatings } from './ratings.js';

export async function initApp(): Promise<void> {
  // ... existing code (load config, load playlists, etc.)
  await loadRatings();
  // ... rest of init
}
```

- [ ] **Step 4: Écrire les tests frontend pour ratings.ts**

```typescript
// static/ratings.test.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { state } from './state.js';
import { loadRatings, getRating, saveRating, deleteRating } from './ratings.js';

// Mock api
vi.mock('./api.js', () => ({
  api: vi.fn(),
}));

import { api } from './api.js';

describe('ratings', () => {
  beforeEach(() => {
    state.ratings = {};
    vi.clearAllMocks();
  });

  it('loadRatings populates state.ratings', async () => {
    (api as any).mockResolvedValue({ '/path/a.mp3': 85, '/path/b.mp3': 72 });
    await loadRatings();
    expect(state.ratings['/path/a.mp3']).toBe(85);
    expect(state.ratings['/path/b.mp3']).toBe(72);
  });

  it('getRating returns value or undefined', () => {
    state.ratings['/path/a.mp3'] = 85;
    expect(getRating('/path/a.mp3')).toBe(85);
    expect(getRating('/nonexistent')).toBeUndefined();
  });

  it('saveRating sends PUT and updates optimistically', async () => {
    (api as any).mockResolvedValue({ ok: true });
    const result = await saveRating('/path/song.mp3', 85);
    expect(result).toBe(true);
    expect(state.ratings['/path/song.mp3']).toBe(85);
    expect(api).toHaveBeenCalledWith('/ratings', {
      method: 'PUT',
      body: JSON.stringify({ '/path/song.mp3': 85 }),
    });
  });

  it('saveRating rolls back on error', async () => {
    (api as any).mockResolvedValue({ ok: false });
    const result = await saveRating('/path/song.mp3', 85);
    expect(result).toBe(false);
    expect(state.ratings['/path/song.mp3']).toBeUndefined();
  });

  it('saveRating validates range', async () => {
    const result = await saveRating('/path/song.mp3', 150);
    expect(result).toBe(false);
    expect(api).not.toHaveBeenCalled();
  });

  it('deleteRating removes key and sends null', async () => {
    state.ratings['/path/song.mp3'] = 42;
    (api as any).mockResolvedValue({ ok: true });
    const result = await deleteRating('/path/song.mp3');
    expect(result).toBe(true);
    expect(state.ratings['/path/song.mp3']).toBeUndefined();
    expect(api).toHaveBeenCalledWith('/ratings', {
      method: 'PUT',
      body: JSON.stringify({ '/path/song.mp3': null }),
    });
  });

  it('deleteRating rolls back on error', async () => {
    state.ratings['/path/song.mp3'] = 42;
    (api as any).mockResolvedValue({ ok: false });
    const result = await deleteRating('/path/song.mp3');
    expect(result).toBe(false);
    expect(state.ratings['/path/song.mp3']).toBe(42);
  });

  it('saveRating overwrites existing value', async () => {
    state.ratings['/path/song.mp3'] = 50;
    (api as any).mockResolvedValue({ ok: true });
    await saveRating('/path/song.mp3', 90);
    expect(state.ratings['/path/song.mp3']).toBe(90);
  });
});
```

- [ ] **Step 5: Exécuter les tests frontend ratings**

```bash
cd /home/giak/projects/audio-sync-tool
npx vitest run static/ratings.test.ts
```

Expected: All tests pass.

- [ ] **Step 6: Commit**

```bash
git add static/state.ts static/ratings.ts static/ratings.test.ts static/actions.ts
git commit -m "feat(frontend): ratings module (CRUD, optimistic update, tests)"
```

---

### Task 3: Frontend — Rendu de la note dans la playlist + édition inline

**Files:**
- Modify: `static/render.ts` — modifier `renderPlaylistTracks()` pour afficher la note + input inline
- Modify: `static/style.css` — styles pour la note et le champ d'édition
- Modify: `static/script.ts` — ajouter la touche N dans le routage mode Playlist (sidebar)

**Interfaces:**
- Consumes: `ratings.js` (getRating, saveRating, deleteRating)
- Consumes: `state.js` (playlistFocus, ratings)
- Produces: DOM elements for rating display + inline edit

- [ ] **Step 1: Ajouter l'affichage de la note dans renderPlaylistTracks()**

Dans `static/render.ts`, modifier la fonction `renderPlaylistTracks()` :

Ajouter l'import :
```typescript
import { getRating } from './ratings.js';
```

Dans la boucle `tracks.forEach((track, i) => { ... })`, ajouter après la durée et avant le bouton remove :

```typescript
// Rating display
const rating = getRating(track.fullPath);
html += `<span class="pl-track-rating" data-fullpath="${escapeHtml(track.fullPath)}">`;
if (rating !== undefined) {
  html += `${rating}`;
} else {
  html += `<span class="pl-track-rating-none">—</span>`;
}
html += `</span>`;
```

Le `<span class="pl-track-rating">` servira de conteneur pour le mode édition.

- [ ] **Step 2: Ajouter la fonction startRatingEdit + handlers dans render.js**

Ajouter après `renderPlaylistTracks()` :

```typescript
import { saveRating, deleteRating } from './ratings.js';

// ── Rating inline edit ────────────────────────────────────────────────────

let _ratingEditActive = false;  // empêche les doubles appels N

/**
 * Enter inline edit mode for the rating of the currently focused track.
 * Called when N is pressed in the sidebar.
 */
export function startRatingEdit(): void {
  if (_ratingEditActive) return;
  const focused = document.querySelector('#playlist-tracks .focused') as HTMLElement | null;
  if (!focused) return;

  const ratingSpan = focused.querySelector('.pl-track-rating') as HTMLElement | null;
  if (!ratingSpan) return;

  const fullPath = ratingSpan.dataset.fullpath || '';
  if (!fullPath) return;

  _ratingEditActive = true;

  const currentRating = getRating(fullPath);
  const input = document.createElement('input');
  input.type = 'text';
  input.className = 'pl-rating-input';
  input.maxLength = 3;
  input.value = currentRating !== undefined ? String(currentRating) : '';
  input.spellcheck = false;

  // Replace the rating span with the input
  ratingSpan.replaceWith(input);
  input.focus();
  input.select();

  function commit(): void {
    const val = input.value.trim();
    if (val === '') {
      deleteRating(fullPath).then(ok => {
        if (!ok) showToast('⚠️ Note non supprimée');
      });
    } else {
      const num = parseInt(val, 10);
      if (!Number.isNaN(num) && num >= 0 && num <= 100) {
        saveRating(fullPath, num).then(ok => {
          if (!ok) showToast('⚠️ Note non sauvegardée');
        });
      }
      // Si valeur invalide, annuler silencieusement
    }
    finish();
  }

  function cancel(): void {
    finish();
  }

  function finish(): void {
    _ratingEditActive = false;
    // Re-render the playlist panel to restore display
    renderPlaylistPanel();
  }

  input.addEventListener('keydown', (e: KeyboardEvent) => {
    if (e.key === 'Enter') {
      e.preventDefault();
      commit();
    } else if (e.key === 'Escape') {
      e.preventDefault();
      cancel();
    }
  });

  input.addEventListener('blur', () => {
    // Validate on blur — same as Enter
    const val = input.value.trim();
    if (val === '') {
      deleteRating(fullPath);
    } else {
      const num = parseInt(val, 10);
      if (!Number.isNaN(num) && num >= 0 && num <= 100) {
        saveRating(fullPath, num);
      }
    }
    finish();
  });
}

function showToast(msg: string): void {
  const el = document.getElementById('status-text') as HTMLElement | null;
  if (!el) return;
  el.textContent = msg;
  clearTimeout((el as unknown as { _toastTimer?: ReturnType<typeof setTimeout> })._toastTimer);
  (el as unknown as { _toastTimer: ReturnType<typeof setTimeout> })._toastTimer = setTimeout(() => {
    if (state.playlistMode) {
      el.textContent = '🎵 Mode Playlist — Espace pour ajouter/retirer, Ctrl+S pour sauvegarder.';
    }
  }, 3000);
}
```

- [ ] **Step 3: Ajouter la touche N dans script.ts**

Dans `static/script.ts`, dans la section mode Playlist du routage clavier, ajouter après les handlers Ctrl+↑↓ et avant le handler ↑↓ :

```typescript
// N — edit rating on focused track (sidebar only)
if (e.key === 'n' && !isInput && state.playlistFocus === 'sidebar') {
  e.preventDefault();
  startRatingEdit();
  return;
}
```

Ajouter l'import :
```typescript
import { startRatingEdit } from './render.js';
```

- [ ] **Step 4: Ajouter les styles CSS dans style.css**

```css
/* ===== Ratings ========================================================== */

/* Rating display in playlist track */
.pl-track-rating {
  font-size: 12px;
  color: var(--text-secondary);
  min-width: 28px;
  text-align: right;
  font-variant-numeric: tabular-nums;
  cursor: pointer;
}
.pl-track-rating-none {
  color: var(--text-dim);
}
.pl-track:hover .pl-track-rating {
  color: var(--text-primary);
}

/* Rating input (inline edit) */
.pl-rating-input {
  width: 32px;
  padding: 1px 4px;
  font-size: 12px;
  font-family: inherit;
  text-align: center;
  background: var(--bg-focus);
  color: var(--text-primary);
  border: 1px solid var(--border-active);
  border-radius: 2px;
  outline: none;
}
.pl-rating-input:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 1px var(--accent);
}
```

- [ ] **Step 5: Vérifier les variables CSS utilisées**

Vérifier que les variables CSS `--text-secondary`, `--text-dim`, `--text-primary`, `--bg-focus`, `--border-active`, `--accent` existent dans `style.css`. Si ce n'est pas le cas, les remplacer par les couleurs hexadécimales du thème existant :

```css
/* Fallback si les variables CSS custom ne sont pas définies : */
.pl-track-rating { color: #a6adc8; }   /* text-secondary equivalent */
.pl-track-rating-none { color: #585b70; } /* text-dim equivalent */
.pl-rating-input { background: #45475a; color: #cdd6f4; border-color: #89b4fa; }
.pl-rating-input:focus { border-color: #89b4fa; box-shadow: 0 0 0 1px #89b4fa; }
```

- [ ] **Step 6: Valider : build + typecheck + tests**

```bash
cd /home/giak/projects/audio-sync-tool
npm run build         # Build OK ?
npx tsc --noEmit       # 0 errors ?
npm test               # 282/282 pass ?
npx biome check --diagnostic-level=error static/src/  # 0 errors ?
```

- [ ] **Step 7: Commit**

```bash
git add static/render.ts static/script.ts static/style.css
git commit -m "feat(frontend): rating display + inline edit (N key) in playlist sidebar"
```

---

### Task 4: Integration + smoke test

**Objectif** : Valider manuellement le workflow complet dans le navigateur.

**Prérequis** :
- Serveur lancé : `cd /home/giak/projects/audio-sync-tool && ./venv/bin/python app.py`
- Navigateur ouvert sur `http://localhost:8765`
- Source Data configuré avec au moins 2-3 fichiers
- Mode Playlist activé, playlist avec des morceaux

- [ ] **Cas 1 — Note affichée dans la playlist**
  1. Mode Playlist, ajouter des morceaux
  2. ✅ Chaque morceau affiche `—` en bout de ligne (note absente)
  3. Si un fichier a une note dans ratings.json → sa note s'affiche

- [ ] **Cas 2 — Édition de note (touche N)**
  1. Tab → focus sidebar playlist
  2. ↑↓ sur un morceau
  3. **N** → ✅ input apparaît, pré-rempli avec la note existante ou vide
  4. ✅ L'input a le focus
  5. Taper `85` → **Entrée** → ✅ la note s'affiche : `85`
  6. ✅ La note est persistée : vérifier `cat data/ratings.json`

- [ ] **Cas 3 — Annulation (Échap)**
  1. **N** sur un morceau noté `85`
  2. Taper `90` → **Échap** → ✅ retour à l'affichage, note inchangée (`85`)

- [ ] **Cas 4 — Suppression de note**
  1. **N** sur un morceau noté
  2. Effacer le contenu → **Entrée** → ✅ affiche `—`
  3. ✅ La clé est supprimée de `ratings.json`

- [ ] **Cas 5 — Validation automatique au blur**
  1. **N** sur un morceau
  2. Taper une valeur
  3. Cliquer ailleurs (perte de focus) → ✅ la note est sauvegardée automatiquement

- [ ] **Cas 6 — Note 0 (distincte de « pas noté »)**
  1. **N** → taper `0` → **Entrée** → ✅ affiche `0` (pas `—`)
  2. Les autres morceaux non notés affichent `—`

- [ ] **Cas 7 — Navigation après édition**
  1. Noter un morceau
  2. ↑↓ → ✅ le focus passe au morceau suivant
  3. **N** → ✅ l'input s'ouvre pour ce morceau

- [ ] **Cas 8 — Note hors limites**
  1. **N** → taper `150` → **Entrée** → ✅ annulation silencieuse, note inchangée
  2. **N** → taper `abc` → **Entrée** → ✅ annulation silencieuse

- [ ] **Cas 9 — Persistance entre sessions**
  1. Noter plusieurs morceaux
  2. Rafraîchir la page (F5)
  3. Mode Playlist → ✅ les notes sont restituées

- [ ] **Cas 10 — Mode normal préservé**
  1. Échap → quitter le mode Playlist
  2. ✅ L'interface normale (Éparpillé + Source Data) fonctionne parfaitement
  3. ✅ F5, Scan, Config, Journal — tout est inchangé
  4. Les notes n'apparaissent pas dans le panneau Source Data (V2)

- [ ] **Step 1: Commit final**

```bash
git add -A && git commit -m "chore: finalize ratings feature"
```

---

## Spec Coverage Check

| Spec Requirement | Task |
|-----------------|------|
| `GET /ratings` | Task 1, Step 3 |
| `PUT /ratings` (save) | Task 1, Step 3 |
| `PUT /ratings` (null = delete) | Task 1, Step 3 |
| Validation 0-100 côté serveur | Task 1, Step 3 |
| Tests backend (6 tests) | Task 1, Step 2 |
| `state.ratings: Record<string, number>` | Task 2, Step 1 |
| `ratings.ts` module (loadRatings, saveRating, getRating, deleteRating) | Task 2, Step 2 |
| Optimistic update + rollback | Task 2, Step 2 |
| `loadRatings()` dans `initApp()` | Task 2, Step 3 |
| Tests frontend (7 tests) | Task 2, Step 4 |
| Affichage note `nn` ou `—` dans la playlist | Task 3, Step 1 |
| Input inline sur touche N | Task 3, Step 2 |
| Entrée = valide, Échap = annule, blur = valide | Task 3, Step 2 |
| Touche N dans script.ts (sidebar only) | Task 3, Step 3 |
| Styles CSS rating | Task 3, Step 4 |
| Build + typecheck + tests | Task 3, Step 6 |
| Smoke test complet (10 cas) | Task 4 |
